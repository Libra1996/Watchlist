#pragma once

#include <vector>
#include <string>

using namespace std;

class Watchlist
{
    private:
        // vector<EntryData> Data;
        // CsvHandler: CsvHandler*;
        // string Mode;
    public:
        // virtual void Data();
        // virtual void Add();
        // virtual void Edit();
        // virtual int Search(string param);
        // virtual void Delete();
        // virtual void GetCsvData();
};
